<!DOCTYPE html>
<html lang="id" data-theme="light">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= konten.judul %> | Portal Alumni</title>
        <link href="https://cdn.jsdelivr.net/npm/daisyui@4.12.10/dist/full.min.css" rel="stylesheet" />
        <script src="https://cdn.tailwindcss.com"></script>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    </head>
    <body class="bg-[#F8F9FA] min-h-screen">
        <%- include('alumni-partials/navbar') %>

        <section class="container mx-auto py-10 px-4 max-w-4xl">
            <div class="bg-white shadow-lg rounded-lg overflow-hidden">
                <% if (konten.gambar) { %>
                <img src="/uploads/<%= konten.gambar %>" alt="<%= konten.judul %>" class="w-full h-96 object-cover" />
                <% } %>

                <div class="p-6">
                    <h1 class="text-3xl font-bold text-green-900 mb-4"><%= konten.judul %></h1>

                    <div class="flex items-center text-sm text-gray-500 mb-6">
                        <span class="mr-4"> <i class="fas fa-user mr-1"></i> <%= konten.author %> </span>
                        <span> <i class="fas fa-calendar-alt mr-1"></i> <%= tanggal %> </span>
                    </div>

                    <div class="text-base text-gray-700 leading-relaxed"><%- konten.isi.replace(/\n/g, '<br />') %></div>
                </div>
            </div>
        </section>

        <%- include('alumni-partials/footer') %>
    </body>
</html>
